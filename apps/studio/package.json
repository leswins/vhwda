{
  "name": "studio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity schema deploy",
    "deploy:studio": "sanity deploy",
    "typegen": "sanity typegen generate",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint not configured yet\" && exit 0",
    "migrate:hard-filters": "tsx scripts/migrate-hard-filters.ts"
  },
  "dependencies": {
    "@sanity/client": "^6.29.0",
    "@sanity/icons": "^3.7.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sanity": "^3.78.0",
    "styled-components": "^6.1.15"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
