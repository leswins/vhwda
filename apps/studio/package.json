{
  "name": "studio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity schema deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint not configured yet\" && exit 0"
  },
  "dependencies": {
    "@sanity/icons": "^3.7.0",
    "sanity": "^3.78.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}


